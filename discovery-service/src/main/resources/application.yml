# Eureka Server ? 8761 ??? Expose
server:
  port: 8761

# Eureka MicorService ? ??? ?? (id)
spring:
  application:
    name: discovery-service

# Eureka Client ??
# discovery-service? EurekaServer ??? ???, client ??? false ??? ?.
eureka:
  client:
    # ?? Server? Eureka Client ? ???? ?? -> fasle
    register-with-eureka: false
    # ?? Server? Eureka Server??? ?????? ??? ????? ???? ?? ?? -> false
    fetch-registry: false



